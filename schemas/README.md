# Schemas

Empty until Phase 4. Will hold `accessibility-profile.schema.json` and the character schema.

Imported profile and character data is validated against these before being applied — imported JSON is untrusted input (plan section 33).
